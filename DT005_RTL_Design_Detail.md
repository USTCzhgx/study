# DT005 RTL 详细设计说明

> 本文以当前 `rtl/` 目录中的实现为准，说明数据通路、存储组织、指针跳转、前瞻、仲裁和出口发送逻辑。设计固定为 8 个输入通道、8 个输出通道、500 MHz，不做参数化；APB 暂不包含在本文范围内。

## 1. 设计目标与基本约束

DT005 是一个 8×8 的报文交换结构。每个输入通道都带有独立的缓存和高低优先级队列，报文最终由目的出口的轮询仲裁器选择并输出。

当前设计遵循以下接口语义：

- 输入通道的 `i_pkt_id_n[2:0]` 是目的出口编号。
- 输出通道的 `o_pkt_id_n[2:0]` 是本次输出报文的源输入通道编号。
- `qos=1` 表示高优先级，`qos=0` 表示低优先级。
- 报文长度必须为 4～128 byte。
- 输入流量约束由系统环境保证：任意连续 8192 个时钟周期内，每个输入通道最多输入 1024 byte。
- RTL 当前不额外实现动态满检测；写读指针不碰撞依赖上述流量约束、远端跳转策略和旧窗口强制排空机制。
- RF 存储器深度为 1024，字宽为 9 bit，读延迟为 1 拍。
- 调度粒度是整包。出口一旦选中某个输入通道，就保持该输入直到报文 EOP 被消费。

## 2. 总体结构

顶层模块为 [`DT005`](rtl/DT005.v)，内部包含 8 个 [`PKT_INGRESS`](rtl/PKT_INGRESS.v) 和 8 个 [`PKT_EGRESS`](rtl/PKT_EGRESS.v)。

```text
输入端口 0 ──> PKT_INGRESS 0 ──┐              ┌──> PKT_EGRESS 0 ──> 输出端口 0
输入端口 1 ──> PKT_INGRESS 1 ──┤              ├──> PKT_EGRESS 1 ──> 输出端口 1
                    ...         ├─ 8×8 交换矩阵 ┤          ...
输入端口 7 ──> PKT_INGRESS 7 ──┘              └──> PKT_EGRESS 7 ──> 输出端口 7
```

每个 `PKT_INGRESS` 完成以下工作：

1. 检查输入报文 SOP/EOP 和长度是否合法。
2. 按 QoS 将数据从两个方向写入一块 1024×9 RF。
3. 只向读侧开放已经完整提交的报文。
4. 从 RF 中前瞻读取数据，解析目的出口 ID。
5. 向唯一的目的出口发出 `ready` 请求。
6. 收到对应出口的授权后，逐字节输出整包。

每个 `PKT_EGRESS` 完成以下工作：

1. 接收 8 个入口对本出口的请求。
2. 在包边界使用轮询算法当拍产生授权。
3. 锁存获胜的源入口，持续读取直到 EOP。
4. 输出数据，并将源入口编号作为输出 `pkt_id`。

## 3. 顶层 DT005

### 3.1 外部端口

顶层外部接口已展开为独立端口，例如：

```verilog
i_pkt_data_0, i_pkt_sop_0, i_pkt_eop_0, i_pkt_qos_0, i_pkt_id_0
o_pkt_data_0, o_pkt_sop_0, o_pkt_eop_0, o_pkt_qos_0, o_pkt_id_0,
o_pkt_vld_0, o_pkt_error_0
```

通道 1～7 使用相同形式。顶层内部再将这些独立端口整理为总线，便于固定的 generate 结构连接。`o_pkt_error_n` 对应输入通道 n 的输入格式错误，不是出口发送错误。

### 3.2 全局时间戳

顶层产生一个 13 bit 自由运行计数器：

```text
0, 1, 2, ... , 8191, 0, 1, ...
```

它是全局时间基准，但每个入口独立记录自己的 `window_base`。因此，一个入口的 8192 周期窗口从该入口第一次接受有效字节时开始，而不是所有入口必须同时开始。

### 3.3 两次矩阵转置

入口输出的请求按“入口视角”组织：

```text
ingress_ready_matrix[入口][出口]
```

出口仲裁需要按“出口视角”看到请求，因此顶层转置为：

```text
egress_ready_matrix[出口][入口]
```

出口生成的读取授权按以下形式组织：

```text
egress_rd_matrix[出口][入口]
```

顶层再次转置，把每个入口收到的 8 个出口授权组合为：

```text
ingress_rd_matrix[入口][出口]
```

数据、SOP、EOP、QoS 和 valid 不需要做目的选择。每个入口的输出数据作为一条候选 lane 广播给全部出口，出口只对 RR 选中的入口 lane 做多路选择。

## 4. PKT_INGRESS

[`PKT_INGRESS`](rtl/PKT_INGRESS.v) 是一个入口通道的二级模块，内部模块关系如下：

```text
输入报文
   │
   ├── DIN_FLT ──> wr_en / pkt_commit / pkt_error
   │                    │
   │                    v
   ├──────────────> WR_CTRL ──> RF_MEM 写地址
   │                    │
   │                    └──> 提交边界、跳转断点、旧窗口边界
   │
   └─ data/id/eop 编码 ───────> RF_MEM(1024×9)
                                    │ 1拍同步读
                                    v
                              RD_CTRL <──> PF_CTRL
                                           │
                                           ├──> ready_to_egress[7:0]
                                           └──> data/sop/eop/qos/vld
```

这里没有 `RAM_CTRL`。`DIN_FLT`、`WR_CTRL`、`RD_CTRL`、`RF_MEM` 和 `PF_CTRL` 都是 `PKT_INGRESS` 的同级子模块。

### 4.1 DIN_FLT：输入合法性检查

[`DIN_FLT`](rtl/DIN_FLT.v) 只观察 SOP/EOP，并维护 `IDLE/RECV` 两个状态及字节计数器。它不处理数据、ID、QoS 或存储地址。

主要规则如下：

| 输入情况 | 结果 |
|---|---|
| `IDLE` 中收到 SOP 且无 EOP | 接受首字节，进入 `RECV` |
| EOP 出现在第 4～128 字节 | 接受当前字节，产生 `pkt_commit` |
| 长度小于 4 | `pkt_error`，不提交 |
| 第 128 字节仍无 EOP | `pkt_error`，终止当前坏包 |
| 空闲时收到孤立 EOP | `pkt_error` |
| 包内再次出现 SOP | 旧半包作废；若新 SOP 合法，当前拍同时作为新包首字节写入 |
| SOP 与 EOP 同拍 | 非法，因为最小报文长度为 4 |

三个输出信号含义：

- `wr_en`：当前输入字节可以写入 RF。
- `pkt_commit`：当前字节是合法报文 EOP，整包可以提交给读侧。
- `pkt_error`：当前输入破坏了报文格式，写指针需要回退或重新开始。

### 4.2 RF 第 9 位编码

RF 每个存储字为 `{info, data[7:0]}`。由于报文长度至少为 4 byte，单独的第 9 位可以在前三个字节存 ID，之后存 EOP：

| 报文字节位置 | RF bit[8] | RF bit[7:0] |
|---|---|---|
| 第 1 字节 | `pkt_id[2]` | data[7:0] |
| 第 2 字节 | `pkt_id[1]` | data[7:0] |
| 第 3 字节 | `pkt_id[0]` | data[7:0] |
| 第 4 字节及以后 | `eop` | data[7:0] |

这使一块 1024×9 RF 同时保存数据、目的出口和包尾信息，无需额外的 ID RAM 或 EOP RAM。

QoS 不写进第 9 位。高低优先级由物理写入方向、读队列选择和 RF 请求对应的 QoS 标签共同确定。

### 4.3 WR_CTRL：写指针与提交边界

[`WR_CTRL`](rtl/WR_CTRL.v) 使用一块 RF 构造两个相向增长的逻辑队列：

```text
低优先级：0    -> 1    -> 2    -> ...
高优先级：1023 -> 1022 -> 1021 -> ...
```

`wr_addr_l/h` 是内部“下一可写地址”，只用于写控制；唯一送给 RF 的写地址是 `mem_wr_addr`。因此不存在重复导出 `mem_wr_addr` 和 `wr_addr_h/l` 的问题。

`wr_addr_last_l/h` 不是当前写地址，而是已经提交数据的边界，也就是相应队列在当前连续区域中的“下一未提交地址”。二者用途不同：

- `wr_addr_*` 可以在半包写入时持续前进。
- `wr_addr_last_*` 只有合法 EOP 到达时才更新。
- RD 只使用提交边界判断可读范围，因此不会读取未提交的半包。

写状态为：

- `WR_IDLE`：当前没有未完成报文。
- `WR_LOW`：正在写低优先级报文。
- `WR_HIGH`：正在写高优先级报文。

正常报文首拍使用输入 `qos` 选择方向；包内后续指针操作由写状态锁定。输入协议仍要求同一个正常报文内 QoS 保持不变。

#### 合法提交

收到合法 EOP 时：

1. 当前字节写入 RF。
2. 相应写指针再移动一格。
3. `wr_addr_last_*` 更新为移动后的地址。
4. 状态返回 `WR_IDLE`。

#### 错包回退

半包尚未提交时，读侧完全看不到它。若发生错误，当前写指针回到相应 `wr_addr_last_*`，原半包占用的地址可直接覆盖。

重复 SOP 是特殊情况：旧包回退，同时当前拍作为新包首字节写入。旧包所属 QoS 由原写状态判断，新包所属 QoS 使用当前输入 `qos`。

### 4.4 8192 周期窗口

每个入口在第一次接受 `wr_en` 时保存：

```text
window_base = global_timestamp
```

全局时间戳模 8192 回到相同数值时，`window_tick` 有效。只要入口还有有效数据、预取数据、正在写的包或当前拍写入，就产生一次 `jump_pulse`。

若入口已完全为空，则没有必要制造物理断点。此时 `ptr_reset` 将高低读写指针恢复到初始的 1023/0，并关闭当前窗口；下一个报文重新启动一个 8192 周期窗口。

### 4.5 固定远端跳转目标

设计使用两组固定起点：

| 目标组 | 低优先级起点 | 高优先级起点 |
|---|---:|---:|
| 外侧组 | 0 | 1023 |
| 中间组 | 512 | 511 |

选择原则是远离当前两个读指针。当前 RTL 判断：

```verilog
rd_both_mid = (^rd_addr_h[9:8]) && (^rd_addr_l[9:8]);
```

地址最高两位为 `01` 或 `10` 时异或为 1，对应地址 256～767。只有高、低两个读指针都位于中间区域时，跳到外侧组 `high=1023, low=0`；其他情况跳到中间组 `high=511, low=512`。

该规则的目的不是镜像当前写指针，而是在两个固定目标组之间选择更远的一组，以降低新写区域追上尚未排空旧数据的风险。

### 4.6 跳转当拍语义

`jump_pulse` 当拍若有有效输入字节，该字节直接写入 `tp_e`，不会再写旧区域：

```text
tp_s = 跳转前的下一可写地址
当前有效字节写到 tp_e
新的下一可写地址 = tp_e ± 1
```

这样做的关键是把跳转时刻定义成清晰的物理断点。WR 和 RD 使用完全相同的 `tp_s -> tp_e` 定义，读侧遇到 `tp_s` 时跳到 `tp_e`，不会访问断点地址。

若跳转时正有一个未完成报文，该报文仍归属于旧窗口。它在旧区域的前半段和 `tp_e` 后的后半段需要作为一个整体排空。`cross_jump` 只在 WR 内跟踪“当前未提交报文是否跨过断点”：

- `jump_pulse` 发生且当前有半包：置位。
- 跨界报文合法提交：清零，并把旧窗口边界扩展到跨界报文 EOP 后。
- 跨界报文出错：清零，回退跨界部分。

`cross_jump` 不能代替 RD 的 `rd_jump_pending_h/l`，因为二者生命周期不同：前者跟随当前半包，后者必须一直保留到读指针真正越过断点。

### 4.7 force_drain：旧窗口强制排空

跳转后，WR 可以继续在新区域接收数据，但 RD 暂时只允许读取旧窗口。`force_drain` 从 `jump_pulse` 开始保持，直到以下条件同时满足：

1. 高低优先级旧窗口的 RF 数据都已发起预取，即 `old_fetch_done`。
2. 跳转时正在进行的跨界报文已经提交或报错，即 `drain_wait_wr_packet=0`。
3. RF 输出和前瞻 FIFO 中属于旧窗口的数据都已经输出，即 `pf_empty=1`。

强排结束前，RD 使用冻结的 `old_end_addr_h/l`；强排结束后再切回持续移动的 `wr_addr_last_h/l`。这样新窗口即使已经有报文提交，也不会混入旧窗口强排过程。

## 5. RD_CTRL：纯读指针控制

[`RD_CTRL`](rtl/RD_CTRL.v) 被刻意限制为“物理读地址控制器”。它不解析 ID，不保存报文数据，也不负责出口状态机。

### 5.1 接口职责

PF 提供：

- `fetch_en`：请求读取一个 RF 字。
- `fetch_qos`：本次读取高优先级还是低优先级队列。

RD 返回：

- `fetch_vld_h/l`：对应队列是否存在已提交数据可预取。
- `ram_rd_en`、`ram_rd_addr`：真正送到 RF 的读请求。
- `rd_addr_h/l`：两个队列下一次要预取的物理地址。

因此 `rd_addr_h/l` 不是出口已经消费到的位置。它们在 RF 读请求被接受时前进，可能比出口实际输出位置领先最多数个字。

### 5.2 可读边界

正常模式下：

```text
selected_end_addr = wr_addr_last
```

强排模式下：

```text
selected_end_addr = old_end_addr
```

只有 `rd_addr != selected_end_addr` 时才有已提交数据可取。因为 WR 只在 EOP 更新提交边界，所以 RD 不会读取未提交半包。第一个报文也必须等提交后，前三个字节才会通过同步 RF 进入前瞻路径。

### 5.3 真实读指针移动

普通读取时：

```text
高优先级：rd_addr_h = rd_addr_h - 1
低优先级：rd_addr_l = rd_addr_l + 1
```

只有 `fetch_en && selected_fetch_vld` 时形成 `fetch_fire`，并产生 `ram_rd_en`。出口消费前瞻 FIFO 不直接推动 RD 指针。

### 5.4 读侧跳转标志

一次全局 `jump_pulse` 同时建立高、低两个物理断点，因此 RD 内部有两个独立的持久标志：

```text
rd_jump_pending_h
rd_jump_pending_l
```

其生命周期为：

```text
jump_pulse                           -> 置 1
对应读指针真正执行 tp_s -> tp_e     -> 清 0
ptr_reset                            -> 清 0
```

高低队列的读取进度不同，一个队列可能很快跨越断点，另一个很久以后才到达，所以不能共用一个瞬时 `jump_pulse` 或一个全局 pending 位。

### 5.5 jump_take 与 jump_skip

当某个队列存在 `rd_jump_pending` 且其下一预取地址等于 `tp_s` 时，`tp_s` 本身不应被访问。

若 `tp_e != selected_end_addr`，说明目标区域有数据：

```text
ram_rd_addr = tp_e
下一 rd_addr = tp_e ± 1
清除对应 rd_jump_pending
```

这称为 `jump_take`，跳转和读取 `tp_e` 在同一次有效 fetch 中完成。

若 `tp_e == selected_end_addr`，说明该 QoS 队列在新区域为空。此时不应虚假读取 `tp_e`，而是直接把 `rd_addr` 归一到 `tp_e` 并清 pending，这称为 `jump_skip`。

### 5.6 RD 状态输出

- `jump_busy`：高、低任意一个 `rd_jump_pending` 仍有效。它阻止前一个物理断点未处理完时再次创建新断点。
- `old_fetch_done`：强排边界内的数据都已经发起 RF 读取。它只表示“读指针侧完成”，不等价于数据已从出口发送完。
- `rd_mem_empty`：两个读预取指针都追上当前实时提交边界。真正的入口全空还必须同时满足 `pf_empty`。

## 6. PF_CTRL：4×9 前瞻与入口侧调度

[`PF_CTRL`](rtl/PF_CTRL.v) 位于同步 RF 和出口之间。它既解决 RF 一拍读延迟，也负责解析队头目的 ID、选择入口内部 QoS 队列并向目标出口请求仲裁。

### 6.1 为什么是有效 4×9，而代码中只有 3×9 数组

前瞻容量由两部分组成：

```text
RF 的 rd_data 输出寄存器 / pending 响应：1 × 9 bit
PF 内部 data_fifo[0:2]：             3 × 9 bit
总有效前瞻容量：                      4 × 9 bit
```

`ram_rd_pending` 表示 RF 输出寄存器里有一个将在当前周期进入 PF 的有效响应；`ram_rd_qos_pending` 保存这次响应属于哪个 QoS。响应被压入显式 FIFO 的同一拍，可以继续发起下一次 RF 读取，因此 RF 流水不会因为搬运响应而停顿。

这里不能只缓存 ID。出口获得 grant 后需要连续输出真实数据，而 RF 有一拍延迟；若只缓存 ID，报文首部数据仍会出现等待。4×9 前瞻同时保存前三个带 ID 位的数据字和后续数据，使调度信息与数据都提前就绪。

### 6.2 预取侧与输出侧必须各自解析

PF 内部有两个独立位置计数：

- `fetch_info_pos`：解释刚从 RF 返回的 bit[8] 当前是 ID2、ID1、ID0 还是 EOP。
- `output_info_pos`：解释当前从 FIFO 消费的数据处于报文第几个字节。

二者不能共用，因为预取最多领先实际输出 4 个字节。

预取侧用 `assembling_id` 拼出三位目的地址。第三个字节返回后，完整队头 ID 才可用于调度。输出侧则根据 `output_info_pos` 生成 SOP/EOP：

- 第一个被消费字节产生 SOP。
- 从第四字节开始，bit[8] 才解释为 EOP。

### 6.3 入口内部高低优先级选择

新报文的预取只在包边界选择队列：

```text
若高优先级有已提交数据：选择高优先级
否则若低优先级有已提交数据：选择低优先级
```

一旦某个低优先级报文已经被选择并开始预取，即使此后提交了高优先级报文，也不会中途改选。`fetch_qos_lock` 保持到当前报文 EOP，避免把两个物理队列的数据拼成一个报文。

因此高优先级是包边界优先，不是字节级抢占。

### 6.4 current/next 报文上下文

PF 保存两套已经解析的调度上下文：

- `current_pkt_id/qos/valid`：当前准备输出或正在输出的报文。
- `next_pkt_id/qos/valid`：已经前瞻解析出的下一个报文。

ID 是寄存后的，而不是从 RF bit[8] 直接组成一条长组合路径。这样在 500 MHz 下，主要请求路径为“寄存 ID → one-hot ready → RR”，也便于实现相邻报文无气泡切换。

### 6.5 ready 的产生与数据消费

`current_pkt_id` 被译码为 8 bit one-hot：

```text
current_pkt_onehot = 8'b1 << current_pkt_id
```

只有在当前报文 ID 已完整解析且 FIFO 队头仍是该报文 SOP 时，才向目标出口拉高对应的 `ready_to_egress` 位。SOP 被授权消费后，ready 立即撤销，后续整包由出口 `DOUT_FSM` 保持授权，不再重复参与 RR。

出口返回的是 8 bit `rd_en_from_egress`。只有其中与当前目的出口 one-hot 相交时，PF 才执行 `fifo_pop`：

```text
current_grant = |(rd_en_from_egress & current_pkt_onehot)
```

这样即使连接矩阵出现其他出口授权，也不会错误消费该入口的数据。

### 6.6 相邻报文无输出气泡

稳定工作时，PF 会在当前报文输出期间继续预取下一报文。有效 4×9 容量允许在当前 EOP 尚未消费时，同时容纳下一报文用于解析 ID 的前三个字节：显式 FIFO 可保存“当前 EOP + 下一包前两字节”，RF 输出保持第三个字节。

时序关系可概括为：

```text
周期 N：   当前报文 EOP 被消费；下一报文 ID 已完成解析
时钟沿：   next 上下文转为 current；DOUT_FSM 回到 IDLE
周期 N+1： 新 ready 有效，RR 当拍授权，新报文 SOP 当拍输出
```

周期 N 和 N+1 都有有效输出，因此报文之间没有空白输出周期。冷启动不同：缓存为空后，第一个报文必须先完整提交，再让前三个 RF 字经过同步读和 ID 重组路径，之后才可能发起 ready。

### 6.7 pf_empty

`pf_empty` 不仅检查显式 FIFO 计数，还要确认：

- 没有待返回的 RF 读响应。
- 没有正在预取和组装的报文。
- `current` 与 `next` ID 上下文均无效。

因此 `rd_mem_empty && pf_empty` 才能表示入口真正没有任何待输出数据。

## 7. PKT_EGRESS

[`PKT_EGRESS`](rtl/PKT_EGRESS.v) 是一个固定目的出口的二级模块。它不再根据输入报文中的目的 ID 做判断，因为入口 PF 已经只向正确出口产生 one-hot ready。

内部只有两个功能模块：

```text
i_ready[7:0] ──> ARB_RR ── grant ──> DOUT_FSM
                                      │
8 路入口数据/control ────────────────>│──> 本出口数据/control
                                      └──> rd_en[7:0] 返回各入口
```

## 8. ARB_RR：包级当拍轮询仲裁

[`ARB_RR`](rtl/ARB_RR.v) 的接口固定为：

```verilog
i_clk
i_rst_n
i_ready[7:0]
i_schedule_en
o_grant_vld
o_grant[7:0]
```

`o_grant` 是 one-hot。`i_schedule_en=1` 时，组合逻辑在当前拍根据 `i_ready` 直接给出授权，不额外插入一拍 grant 寄存延迟。

### 8.1 轮询算法

`rr_mask` 保存上次获胜入口之后的候选范围：

```text
masked_ready = i_ready & rr_mask

若 masked_ready 非零：在 masked_ready 中选最低编号请求
否则：               回绕，在全部 i_ready 中选最低编号请求
```

例如上一次入口 3 获胜，下一次优先搜索 4、5、6、7；若都没有请求，再回绕搜索 0、1、2、3。

授权后更新 mask，使下一轮从本次获胜者之后开始。入口 7 获胜后 mask 变为 0，下一次自然从入口 0 回绕。

### 8.2 公平性边界

RR 的公平单位是报文，不是字节。因为 `i_schedule_en` 只在 `DOUT_FSM` 空闲时有效，128 byte 长包一旦获胜会连续占用出口直到 EOP；下一次 RR 才轮到其他入口。

出口 RR 不看 QoS。QoS 优先级已经在每个入口的 PF 中用于选择该入口下一次提供高队列还是低队列。多个入口同时请求同一出口时，它们在 RR 层平等轮询。

## 9. DOUT_FSM：出口锁包与数据选择

[`DOUT_FSM`](rtl/DOUT_FSM.v) 使用两个状态：

- `DOUT_IDLE`：出口没有锁定源入口，`o_schedule_en=1`。
- `DOUT_SEND`：已经锁定源入口，持续读取到 EOP。

### 9.1 IDLE 中的当拍调度

在 `DOUT_IDLE`：

1. `o_schedule_en` 组合拉高。
2. ARB_RR 当拍产生 one-hot grant。
3. DOUT 当拍把 `o_rd_en` 直接等于 grant。
4. 同一个入口 PF 当拍看到正确授权并弹出 SOP。
5. DOUT 的多路器当拍选择该入口数据输出。

因此没有“先仲裁一拍、下一拍再读首字节”的固定延迟。

若首字节有效且不是 EOP，时钟沿锁存获胜的源入口编号 `active_id`，进入 `DOUT_SEND`。正常合法报文至少 4 byte，因此通常一定进入 SEND。

### 9.2 SEND 中锁定整包

在 `DOUT_SEND`：

- 不再请求 RR 调度。
- `o_rd_en` 始终是 `1 << active_id`。
- 数据、SOP、EOP、QoS、valid 始终从 `active_id` 对应入口 lane 选择。
- 只有在 `selected_vld && selected_eop` 时回到 IDLE。

如果入口短暂无有效数据，状态不会切换到其他源，而是保持当前源等待。这保证一个出口报文不会被其他入口插入。

### 9.3 输出 ID

`o_pkt_id` 是 RR 选中的源入口编号：

```text
入口 7 的报文发往出口 0：
输入 pkt_id = 0
出口 0 的输出 pkt_id = 7
```

目的 ID 在入口侧只用于选择向哪个出口发请求，数据到达出口后无需再次输出目的 ID。

### 9.4 EOP 后无气泡切换

当前 EOP 在周期 N 输出并于时钟沿使 DOUT 返回 IDLE。周期 N+1：

- `schedule_en` 已有效。
- RR 对下一个 ready 当拍授权。
- 新源的 SOP 当拍输出。

因此 EOP 与下一 SOP 可以出现在相邻周期。设计没有在 EOP 的同一个组合周期提前切换源，但这不会形成空白周期。

## 10. 完整调度层次

当前系统共有三层选择，职责不能混淆：

```text
第一层：入口 PF 在包边界选择高/低 QoS 队列
                    │
                    v
第二层：目的出口 RR 在多个请求入口中轮询选择一个入口
                    │
                    v
第三层：出口 DOUT 锁定该入口，连续发送到 EOP
```

第一层解决同一入口内部高低优先级；第二层解决多个入口争用同一出口；第三层保证整包原子发送。

一个典型流程如下：

1. 输入 5 收到 `pkt_id=2, qos=1` 的完整报文并提交。
2. 输入 5 的 PF 优先预取高队列，读出前三个 9 bit 字并恢复目的 ID 2。
3. 输入 5 仅置位 `ready_to_egress[2]`。
4. 出口 2 的 RR 在所有请求出口 2 的入口中选择输入 5。
5. 出口 2 将 one-hot `rd_en[5]` 返回输入 5。
6. 输入 5 的 PF 弹出数据；DOUT 锁存源 ID 5。
7. 出口 2 连发到 EOP，且输出 `o_pkt_id_2=5`。

## 11. 关键组合路径与 500 MHz 考量

当拍调度的关键路径大致为：

```text
PF 中寄存的 current_pkt_id
  -> ready one-hot
  -> ARB_RR mask + 优先编码
  -> DOUT_FSM grant/active 源选择
  -> rd_en 返回 PF
  -> fifo_pop / PF 队头输出
  -> DOUT 数据多路器
  -> 顶层输出
```

这条路径换取了零额外仲裁拍。当前设计已经避免把同步 RF 读、三位 ID 串行组装或未寄存的写侧信号直接放进该路径；ID 在 PF 中完成并寄存后才参与 ready。

后续用 DC 综合时应重点检查：

- 8 路 RR 优先编码与 mask 逻辑延迟。
- 8 路数据和控制多路器延迟。
- `rd_en -> fifo_pop -> data_vld/data` 的组合路径。
- 顶层两次矩阵转置只应成为连线，不应综合出额外逻辑。
- RF 应替换或映射为目标工艺的 1024×9 单写单读存储宏，确认宏的读延迟和读写同址行为与模型一致。

若 500 MHz 时序不满足，不能简单给调度固定增加一拍，因为这会改变无气泡目标。应先根据 DC 报告定位具体路径，再考虑局部重构，例如优化 one-hot 选择结构、物理布局或使用适配工艺库的 RF 宏。

## 12. 指针跳转的关键不变量

实现和后续修改应持续满足以下不变量：

1. `wr_addr_last` 只表示已提交边界，半包不能推进它。
2. `rd_addr` 只在 RF fetch 被接受时前进，不在出口消费时前进。
3. 跳转当拍的有效写数据直接写 `tp_e`。
4. `tp_s` 是跳转前下一可写地址，是物理断点，不应被 RD 当作普通数据读取。
5. WR 的 `cross_jump` 与 RD 的 `rd_jump_pending_h/l` 必须分离。
6. 高低读队列必须各自完成 `tp_s -> tp_e`，不能因另一个队列完成而清除自己的 pending。
7. 跳转时正在进行的报文属于旧窗口，必须提交或报错后才能结束强排。
8. 强排期间读取冻结的 `old_end_addr`，不能使用仍在随新窗口提交移动的实时边界。
9. `old_fetch_done` 只表示 RF 数据已取完，必须再等 `pf_empty` 才能认为旧数据已真正输出完。
10. 完全空时才允许 `ptr_reset`；当前拍有写入或仍有半包时不得复位指针。
11. 前一个断点仍处于 `jump_busy` 时不能创建第二个断点。
12. 写读不碰撞依赖输入流控限制；当前 RTL 没有完整的动态容量保护电路。

## 13. 复位与空闲行为

初始物理位置为：

```text
低 QoS 读写边界：0
高 QoS 读写边界：1023
```

RF 数组本身不清零，只复位读输出寄存器。因为所有有效性由提交边界、pending、FIFO 计数和上下文 valid 管理，未写地址的旧值不会被当作有效数据；同时避免综合出 1024×9 的逐位复位结构，便于映射存储宏。

当前大部分模块使用低有效异步复位；`DIN_FLT` 的状态与计数实现为时钟内同步判断 `rst_n`。若后续工艺规范要求统一复位方式，应结合 DC 库单元和复位树要求单独调整，不应顺带改变协议功能。

## 14. 当前验证用例

当前 `tb/` 目录包含：

- [`tb_DT005.v`](tb/tb_DT005.v)：基础端到端路由和数据检查。
- [`tb_PKT_EGRESS.v`](tb/tb_PKT_EGRESS.v)：出口 RR、源锁定和连续报文切换检查。
- [`tb_RD_CTRL.v`](tb/tb_RD_CTRL.v)：读指针及断点跳转检查。
- [`tb_WR_CTRL.v`](tb/tb_WR_CTRL.v)：写入、提交、回退与跳转检查。
- [`tb_DT005_QOS_2WINDOW.v`](tb/tb_DT005_QOS_2WINDOW.v)：两轮 8192 周期 QoS 压力场景。

两窗口场景为：

1. 第一轮：输入 0～6 各发送 8 个 128 byte 高优先级包；输入 7 先发送 7 个 128 byte 高优先级包，再发送 32 个 4 byte 低优先级包；全部发往出口 0。
2. 第二轮：8 个输入通道各发送 8 个 128 byte 高优先级包；全部发往出口 0。
3. 所有包在各自允许的流量范围内连续发送，不人为插入包间空拍。

该测试主要覆盖：输入侧 QoS 选择、多个入口对同一出口的包级 RR、8192 周期窗口、远端跳转、旧窗口强排以及相邻包连续输出。

## 15. 模块职责总结

| 模块 | 只负责什么 | 不负责什么 |
|---|---|---|
| `DIN_FLT` | SOP/EOP、4～128 byte 合法性 | 数据、ID、地址、QoS 调度 |
| `WR_CTRL` | 写地址、提交边界、错包回退、写侧跨界 | RF 数据内容、出口仲裁 |
| `RF_MEM` | 1024×9 单写单读存储 | 包协议、ID 解析 |
| `RD_CTRL` | 已提交范围内的预取地址、读侧跨界 | 缓存数据、ID 解析、出口状态 |
| `PF_CTRL` | 4×9 前瞻、ID/EOP 解析、入口 QoS 选择、ready/消费 | 多入口 RR |
| `ARB_RR` | 同一出口上多个入口的包级公平轮询 | QoS 选择、数据保持 |
| `DOUT_FSM` | 当拍接受 grant、锁定源到 EOP、输出复用 | 目的地址解析、入口 QoS 选择 |
| `PKT_INGRESS` | 组合入口子模块、窗口和跳转总控 | 跨入口仲裁 |
| `PKT_EGRESS` | 组合 RR 和出口发送 | 输入缓存管理 |
| `DT005` | 8×8 实例化、时间戳、矩阵连接 | 单通道内部协议细节 |

这种分工的核心是：写侧只管理“数据什么时候成为可读”，读侧只管理“下一次从哪个物理地址取”，PF 管理“取出的报文下一步去哪里”，出口管理“多个入口谁先完整发送”。只要这些边界保持清晰，后续逐模块优化时就不会把提交、预取、仲裁和消费重新耦合在一起。
